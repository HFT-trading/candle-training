[package]
name = "structure-core"
version = "0.1.0"
edition = "2024"

[dependencies]
candle-core = { version = "0.10.2", features = ["metal"] }
candle-nn = "0.10.2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
