[package]
name = "bot-training"
version = "0.1.0"
edition = "2024"

[dependencies]
candle-core = { version = "0.10.2", features = ["metal"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Logger
tracing = "0.1.41"
