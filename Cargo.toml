[workspace]
resolver = "2"
members = ["crates/training"]

# Optimize dependencies (candle/gemm) even in debug builds so `cargo run` is fast
# for tensor work, while keeping our own crates debuggable.
[profile.dev.package."*"]
opt-level = 3
